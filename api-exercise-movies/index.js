const { readList, findMovie, attMovie, deleteMovie } = require('./src/filtros');

function main() {
    //readList();
    //findMovie();
    //attMovie(2, 'teste', 2 );
    deleteMovie("Titanic");
}

main();